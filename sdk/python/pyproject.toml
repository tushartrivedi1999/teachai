[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "rivinity-sdk"
version = "0.1.0"
description = "Python SDK for Rivinity Learning API"
readme = "README.md"
requires-python = ">=3.10"
dependencies = ["httpx>=0.27.0"]
